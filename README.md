# Matrix Primitives

> **Status: Pre-implementation** — This package is a namespace reservation. It declares dependencies on linear, vector, dimension, and index primitives and exposes an empty `Matrix` namespace, but carries no usable implementation yet. No downstream consumers exist.
